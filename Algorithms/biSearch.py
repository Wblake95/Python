import mymath

def search(num):
    for i in range(num):
        search = num
        if mymath.multi(num,num) == search:
            print(mymath.multi(num,num)
            break
        elif mymath.multi(num,num) > search:
            search = num/2
            print(search)
            continue
        elif mymath.multi(num,num) < search:
            search



